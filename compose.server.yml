name: hetznerwatch

services:
  app:
    image: ghcr.io/heiselanalytics/hetznerwatch:latest
    restart: unless-stopped
    ports:
      # Keep the dashboard private. Use an HTTPS reverse proxy for public access.
      - "127.0.0.1:8080:8080"
    volumes:
      - data:/data
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/api/health', timeout=3)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  data:
    name: hetznerwatch-data
